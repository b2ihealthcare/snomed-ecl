/*
 * Filters results by description language code. Useful when combined with description term filtering.
 * The filter's argument needs to be a valid ISO 639-1 language code.
 */

<<404684003|Clinical finding| {{ language = de AND term = "Entzündung" }}
