/*
 * Description term filters allow constraint-based matches to be restricted further, using textual matching.
 *
 * Supported match types are:
 * - exact match
 * - "smart match" (default)
 * - regular expression (an extension to ECL 1.5)
 * - wildcard (an extension to ECL 1.5)
 */

* {{ term = exact : "Disease" }}
